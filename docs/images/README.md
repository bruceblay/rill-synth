# Screenshot provenance

Captured September 21, 2026 from `src/Light.h` and `src/Garden.h` at source revision `997dd0a` (including the Contour-only hit-response fix). Seed 17; actual engine notes and levels drive the renderer. Native output is 240 × 135; the contact sheet uses nearest-neighbor 2× replication.

The tools are `tools/visual_preview.cpp` and `tools/gallery.cpp` on `visuals-study`. Family/frame pairs: Contour 0/120, Pendulum 1/180, Growth 2/110, Eclipse 3/120, Tiles 5/150, Reef 7/300. Compile with C++17, run to produce PPM, then convert losslessly to PNG. No generated or simulated device photography is used.
